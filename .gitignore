# Godot 3.x — кэш импорта (пересоздаётся автоматически при первом открытии)
# Параметры импорта хранятся рядом с ресурсами (*.import) и коммитятся.
.import/

# Мусор ОС / редакторов
Thumbs.db
Desktop.ini
*.tmp
*.log
